version: "3.3"
services:
  app:
    build: .
    restart: always
    environment:
      - VERSION=2.4.0
    image: boristax/wardrobes:v2.4.0
    ports:
      - "5555:5555"
    command: npm run start
    volumes: 
      - wardrobes-db:/app/src/server/database/wardrobes
      - sklad-db:/app/src/server/database/sklad
      - users-db:/app/src/server/database/users
volumes:
  users-db:
      external: true
  wardrobes-db:
      external: true
  sklad-db:
      external: true